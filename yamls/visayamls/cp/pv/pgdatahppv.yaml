apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-hp-volume-prophecy
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 10Gi
  hostPath:
    path: "/ssadfs-a/sphinx/stage/prophecy/pgdata"
  persistentVolumeReclaimPolicy: Retain
  storageClassName: localpath
  volumeMode: Filesystem
